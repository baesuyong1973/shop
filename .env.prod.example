# docker-compose.prod.yml 用の環境変数（本番サーバーの ./ .env としてコピーして値を埋める）
# このファイル自体はコミットしてよいプレースホルダー。実値を入れた .env はコミットしないこと。

DB_PASSWORD=change_me_strong_password
DB_ROOT_PASSWORD=change_me_strong_root_password
